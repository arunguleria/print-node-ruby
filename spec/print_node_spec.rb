require 'spec_helper'

describe PrintNode do
  it 'has a version number' do
    expect(PrintNode::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
